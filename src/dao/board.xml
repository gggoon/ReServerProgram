<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="dao.board">

   <select id="selectBoardList" resultType="dto.BoardDTO">
      SELECT no, title, author, postdate, grade
        FROM board
       ORDER BY no desc 
   </select>
   
  
   <select id="selectBoardMaxHitJob" resultType="dto.board">
          SELECT a.no, a.title, a.contant, a.hit
            FROM (SELECT no, title, content, hit
                    FROM board
                   ORDER BY no DESC) a
  </select>
</mapper>





























